# 注意：這個檔案「取代」.gitignore，不是疊加上去。
# 少寫一條 node_modules 就會把整個 node_modules 上傳，所以下面必須是完整的一份。

node_modules
.pnp
.pnp.*
.yarn

coverage
.next
out
build

.DS_Store
*.pem

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

.env
.env.*
!.env.example

.vercel
*.tsbuildinfo
next-env.d.ts

pnpm-lock.yaml
yarn.lock
bun.lockb

supabase/.temp
supabase/.branches

# Serwist 產生的 service worker。
#
# 這是這個檔案存在的主要理由：public/sw.js 在 .gitignore 裡寫成
# `/public/sw.js`，但 Vercel 的比對不吃這個開頭的斜線，本機建置留下的
# 那份就會被一起上傳，蓋掉 Vercel 自己建出來的版本。
#
# 兩者的差別是致命的：Vercel 會把靜態資源放到 /_next/static/immutable/ 底下，
# 本機不會。所以上傳上去的 manifest 指向的每一個檔案在線上都是 404，
# precache 失敗，Service Worker 永遠停在 installing —— PWA 整個不會生效。
public/sw.js
public/sw.js.map
public/swe-worker-*.js
